apiVersion: v1
kind: Service
metadata:
  name: inverter-dashboard-go
  labels:
    app: inverter-dashboard-go
spec:
  type: ClusterIP
  selector:
    app: inverter-dashboard-go
  ports:
    - name: http
      port: 8080
      targetPort: http
