apiVersion: v1
kind: ConfigMap
metadata:
  name: inverter-dashboard-go
  labels:
    app: inverter-dashboard-go
data:
  # IGW-only: do not set MQTT_HOST/MQTT_PORT (Cerbo MQTT owned by inverter-gateway).
  WEB_PORT: "8080"
  WEB_HOST: "0.0.0.0"
  GATEWAY_ENABLED: "true"
  GATEWAY_URL: "https://victron.2560801.xyz"
  GATEWAY_POLL_INTERVAL_SECONDS: "2"
  # Optional Cerbo portal metadata for water/EV instance mapping from snapshot
  CERBO_PORTAL_ID: "b827ebea1ece"
