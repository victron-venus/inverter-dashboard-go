name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    name: CI
    uses: victron-venus/venus-os-ci-toolkit/.github/workflows/go-ci.yml@98ef67a92a659c1b4633279961ed2bc704a506e2
    with:
      # go-version: '1.23'  # default from workflow
      # working-directory: '.'  # default
      # run-lint: true  # default
      # run-tests: true  # default
      # test-args: '-race -coverprofile=coverage.out -covermode=atomic'  # default
      # coverage-threshold: 70  # default
      # run-vulncheck: true  # default
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
